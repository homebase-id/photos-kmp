# Homebase Photos — Current Handoff (2026-06-22)

**Status:** Batch 0 (skeleton) ~done; first Batch-1 Timeline slice written but **build not yet
re-verified**. Owner halted the autonomous build to hand the **UI/UX to a dedicated redesign effort**
(the first-pass UI was judged inadequate). This doc = where to resume.

> Orientation/reference (don't re-derive): `CLAUDE.md`, original `HANDOFF.md` (reference paths to
> `chat-kmp`, gotchas), `docs/superpowers/specs/2026-06-21-homebase-photos-design.md` (spec),
> `docs/superpowers/plans/2026-06-21-batch-0-skeleton.md` (Batch-0 plan),
> `docs/superpowers/batch1-contracts.md` (locked ViewModel/UiState/repo contracts),
> `docs/design/design-system.md` (current "Conservatory" theme — under review).
> Durable rules + history: `~/.claude/projects/-Users-biswa-Documents-GitHub-homebase-photos/memory/`
> (`MEMORY.md` index + notes).

## ▶ First actions when resuming

1. **`/goal clear`** if any stacked goal hooks are still active from the prior session.
2. **Run the build and confirm green** (it was unverified at halt — two fixes applied, never built):
   - `./gradlew :shared:compileKotlinJvm :shared:compileAndroidMain :shared:compileKotlinIosSimulatorArm64`
   - `./gradlew :shared:jvmTest` — judge ONLY `id.homebase.photos.*` + `id.homebase.api.sync.database.*`
     (125). The ~160 imaging-test failures are **pre-existing** (missing Skia native test dep; see T19).
   - `./gradlew :androidApp:assembleDebug`
   - iOS: `xcodegen generate` in `iosApp/` → `xcodebuild -project iosApp/iosApp.xcodeproj -scheme iosApp
     -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17' build`. If
     `:shared:linkDebugFrameworkIosSimulatorArm64` hits a K/N "No module deserializer" cache crash,
     re-run that task with `--rerun-tasks`.
3. **Run the UI flow tests** (Compose `:androidApp:connectedDebugAndroidTest`, iOS XCUITest) + Argent
   device render check of the timeline grid. Known open: iOS `TimelineGridUITest` couldn't find the
   `timeline-root` a11y element though the view renders — confirm `TimelineView` is the launched root
   (`ContentView`/`iOSApp`) + the accessibility identifier/container wiring. Likely test-only.

## Proven & working (verified at the time)

- Native SwiftUI (iOS) + Jetpack Compose (Android) over a **headless KMP `shared`** = copied+trimmed
  `chat-kmp/homebase-api` (pin `e67130cd`) + the encrypted Coil pipeline from `homebase-common`.
- Toolchain: Gradle 9.5.0, AGP 9.2.1, Kotlin 2.3.21, **SKIE 0.10.12** (Flow→AsyncSequence; do NOT use
  0.10.6), Koin DI, SQLDelight `OdinDatabase`, Ktor, Coil 3, Kermit.
- `:shared` compiles JVM/Android/iOS; 125 inherited DB tests pass.
- `PhotoConfig` (TDD): drive ids, fileType 0/0, album 900, `dflt_key`, thumb dims `[20,300,1200]`,
  **video marker = payload `contentType` `image/*` vs `video/*`**.
- Both **render-proofs render on real devices** (StateFlow via SKIE; Compose UI test + XCUITest passed).
- **`apiModule` boots on both platforms** (`platformModule()` provides `FileOperationsProvider`;
  `initKoin()` boots `allModules`). Verified offline both devices.

## Written, NOT yet build-verified (Timeline wave)

Photos unit tests were green; the integration build verifier was killed before confirming.
- Shared: `id.homebase.photos.domain.PhotoItem`; `data/PhotosRepository(+Impl,+Mock)`;
  `timeline/TimelineViewModel`+`TimelineUiState`+`TimelineSection`; `PhotoQueries`; paged
  `DriveMainIndex.sq` query. `photosModule` binds these (uses `MockPhotosRepository` = ~60 seeded
  photos so the grid renders before login/sync exist — swap for `PhotosRepositoryImpl` once synced).
- Android: `ui/timeline/TimelineScreen.kt` (LazyVerticalGrid, sticky month headers, Coil + encrypted
  fetcher, video badge) + `ui/CoilSetup.kt`; `MainActivity` renders it.
- iOS: `timeline/TimelineView.swift` (LazyVGrid) + `ThumbnailLoader.swift` (actor + NSCache over
  shared `loadThumbnailBytes` via SKIE); `ContentView` renders it.
- Tests: `QueryBatchTagFilterTest`, `VideoMarkerTest`, Compose grid test, iOS `TimelineGridUITest`.
- **Two fixes applied at halt (on disk, unbuilt):** Android `kotlin{compilerOptions{optIn.add("kotlin.uuid.ExperimentalUuidApi")}}` in `androidApp/build.gradle.kts`; top-level `loadThumbnailBytes` shim in `PhotosModule.kt`.

## ⚠ Primary next focus: UI/UX redesign (dedicated instance)

Owner rejected the first-pass look ("absolutely ugly") and wants a **separate, design-led rebuild** —
not tweaks. Standing intent (re-confirm with owner first): **earthy green pastel, chat-kmp visual
inspiration, Google-Photos-class density + polish, photos-first/chrome-recedes.** See memory
`ui-needs-dedicated-redesign` + `design-direction`. Use `superpowers:brainstorming` to align, then
`frontend-design`; validate on devices with the `argent-*` skills.

## Gates (do not skip)

- **Schema sign-off with owner BEFORE any upload/backup file-build code** (Batch-1 backup). See memory
  `discuss-schema-before-upload`.
- **Login:** build runnable on-device, then **ping the owner to perform the login** — no fabricated
  identity (memory `ping-owner-for-login-flow`).
- **Strict TDD** on every logic unit + **a UI flow test per screen**; **code-first / batched verify**
  (writers write only/parallel, ONE verifier builds; never write source while a build runs).

## Remaining roadmap

Auth/login (T8, next) → fullscreen viewer (T13) → manual backup [SCHEMA GATE] (T14) → cross-device
restore (T15) → albums (T16) → video (T17, riskiest). Cleanup: re-add jvmTest desktop Skia dep so the
imaging regression tests go green (T19). UI rebuild cross-cuts all screens.

## Housekeeping

- **Nothing is committed** — entire working tree uncommitted (repo rule). Review + commit when ready.
- Disk is tight (~9 GiB); debug APK ~143 MB (bundled FFmpegKit/sqlcipher). Sim = iPhone 17 / iOS 26.5
  (no iOS 18.x runtime; deployment floor 18.2); Android = `Pixel_9` AVD.
- A Pixel_9 emulator / iPhone 17 sim / Argent server / Gradle daemon may be running — stop with
  `stop-all-simulator-servers`, `./gradlew --stop` if you want resources back. Argent update 0.12.0→
  0.12.1 available (owner consent only).

## Suggested skills

`superpowers:brainstorming` (align UI direction first) · `frontend-design` (the UI rebuild) ·
`superpowers:using-superpowers` · `kmp-compose-multiplatform` + `kotlin-coroutines-flows` · `argent-*`
(device + grid perf) · `superpowers:writing-plans`/`executing-plans` · `superpowers:test-driven-development`.
